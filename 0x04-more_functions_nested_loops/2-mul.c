#include "main.h"

#include <stdio.h>

/**
 * mul - a function that multiplies two integers.
 * @x: my first number
 * @y: my second number
 * Return: always 0
 */
int mul(int x, int y)
{
	int result = x * y;

	return (result);
}
