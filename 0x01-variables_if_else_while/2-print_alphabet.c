#include <stdio.h>
/**
 *main - entery point a program that prints the alphabet in lowercase
 *Return: always 0 (ssuccess)
 */
int main(void)
{
	char n;

	n = 'a';
	while
		(n <= 'z')
		{
			putchar(c);
			n++;
		}
	putchar('\n');
	return (0);
}
