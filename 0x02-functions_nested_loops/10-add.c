#include "main.h"
#include <stdio.h>

/**
 * add - a function that adds two integers and returns the result
 * @x: add the first no
 * @y: add the next no
 * Return: always 0
 */
int add(int x, int y)
{
	return (x + y);
}
